Generate or view session summaries.

Run the summarize command:

```bash
claude-summarize ${ARGUMENTS:-show 10}
```

Commands:
- `update` - Generate summaries for sessions without them
- `show [limit]` - Display recent session summaries (default: 10)

Present the results to the user.
